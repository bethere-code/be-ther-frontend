org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Flutter 3.47 transitional default: keep legacy KGP until all plugins migrate.
# builtInKotlin=true left google_sign_in_android's ResultUtils.kt uncompiled
# (Java then fails with cannot find symbol ResultUtilsKt).
android.builtInKotlin=false
# Keep the legacy android {} types until the remaining plugins migrate off BaseAppModuleExtension.
android.newDsl=false
